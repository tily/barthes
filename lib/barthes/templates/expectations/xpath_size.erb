lambda {
	doc = Nokogiri::XML response.response.body.gsub(/xmlns=\"(.+?)\"/, '')
	<% if respond_to?(:value) %>
	<% if compare == 'eq' %>
	doc.xpath('<%= xpath %>').size == <%= value %>
	<% end %>
	<% if compare == 'gt' %>
	doc.xpath('<%= xpath %>').size > <%= value %>
	<% end %>
	<% end %>
}.call
